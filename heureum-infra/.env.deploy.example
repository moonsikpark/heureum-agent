# Heureum deployment configuration.
# Copy to .env.deploy and fill in real values.
# Used by: scripts/deploy.sh, make deploy-*

# ── Azure / AKS ──────────────────────────────────────────────────────────────
ACR_NAME=youracrname
RESOURCE_GROUP=your-resource-group
CLUSTER_NAME=your-aks-cluster
DOMAIN=heureum.ai

# ── K8s Secrets (used by `deploy.sh aks` to create heureum-secrets) ──────────
OPENAI_API_KEY=
DJANGO_SECRET_KEY=
DATABASE_URL=
AZURE_COMMUNICATION_CONNECTION_STRING=
DEFAULT_FROM_EMAIL=noreply@heureum.ai
FRONTEND_URL=https://heureum.ai
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
AZURE_STORAGE_CONTAINER_NAME=media
ACR_PASSWORD=

# Path to Google Cloud service account JSON (for Gemini / Vertex AI)
GCP_SA_KEY_FILE=./path/to/your-service-account.json

# ── Deploy Options ────────────────────────────────────────────────────────────
# TAG=                          # Image tag (default: git SHA)
# SERVICES="agent platform frontend mcp"   # Which services to build/deploy
# SKIP_BUILD=0                  # Set to 1 to skip docker build/push
